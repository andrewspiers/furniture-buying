# 2026-07-29 11:00
Scaffolded a Next.js (TypeScript) app with Bootstrap/react-bootstrap styling, SQLite via Prisma 6, and NextAuth credentials login. Seeded a demo user (buyer@example.com, $2000 budget) and 8 furniture products. Built catalogue browsing with live order totals, a budget-enforcing order API, and an order-history page with spend tracking. Verified the full flow end-to-end with a headless-browser script: login, place an order, budget updates correctly, over-budget orders are blocked. Fixed one bug along the way — react-bootstrap components need a Client Component boundary, not a Server Component. Test orders cleared before handoff. App builds cleanly, no console errors.

# 2026-07-29 11:44
Wrote ARCHITECTURE.md (Mermaid data model) and requirements.md. Replaced the 8 placeholder products with a real 762-item MongoDB furniture catalogue (prisma/import-catalog.ts, MONGODB_URI in .env only). Extended the Product schema for imported photos (stored as bytes, served via a dedicated image route so the catalogue listing stays lightweight) and source tracking. Found and fixed two real bugs during verification: a client-navigation race after placing an order, and a server/client date-formatting hydration mismatch. All changes type-check and pass a full headless-browser regression of login, browsing, ordering, budget limits, and order history. Not yet committed.

# 2026-07-29 15:01
Explored the Cognitivo training API's OpenAPI schema and added api-testing/ curl scripts for categories, search-index, single-product, and full-catalogue endpoints (the last hangs indefinitely — capped with --max-time 30). Discovered GET /users/{user_id} returns a real live balance. Replaced the app's internal fake budget with this real balance everywhere it's shown or enforced (catalogue page, orders page, order-placement API), added lib/cognitivoApi.ts, and deleted the now-unused lib/budget.ts. Flagged clearly in docs that orders placed here don't call the API's own POST /orders, so the real balance isn't affected by this app's orders. Verified end-to-end with no console errors. Not yet committed.

# 2026-07-29 15:16
Switched catalogue browsing from our own Mongo-synced database to the live search-index endpoint, per instructions (not the plain /catalogue endpoint, which hangs). Catalogue now shows category/name/price for all 762 products in a plain table; dropped the image/description display and the now-unused image-serving route since search-index doesn't provide those. Order placement had to change too: items are now keyed by the API's item_id rather than our internal id, with a fallback that auto-creates a local Product row (for the required DB foreign key) if one doesn't already exist. Verified full flow including that fallback path. Docs updated. Not yet committed.

# 2026-07-29 15:49
Confirmed the earlier real-balance work was intact (user re-asked; nothing had regressed). Then improved order error handling per request: insufficient-balance and product-no-longer-available responses now carry a machine-readable code plus a clear, specific message; wrapped the whole order route in a top-level try/catch so any unexpected failure still returns valid JSON instead of a raw crash; client now guards fetch() and res.json() so a network failure or malformed response can't go unhandled. Verified both error paths render correctly with no console errors, via direct API calls and mocked responses (react-bootstrap's Button doesn't wire onClick at all while disabled, so a normal over-budget click couldn't be forced through the real UI — mocking the endpoint was the clean way to test the client's rendering). Not yet committed.
