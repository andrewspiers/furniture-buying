Scaffolded a Next.js (TypeScript) app with Bootstrap/react-bootstrap styling, SQLite via Prisma 6, and NextAuth credentials login. Seeded a demo user (buyer@example.com, $2000 budget) and 8 furniture products. Built catalogue browsing with live order totals, a budget-enforcing order API, and an order-history page with spend tracking. Verified the full flow end-to-end with a headless-browser script: login, place an order, budget updates correctly, over-budget orders are blocked. Fixed one bug along the way — react-bootstrap components need a Client Component boundary, not a Server Component. Test orders cleared before handoff. App builds cleanly, no console errors.
